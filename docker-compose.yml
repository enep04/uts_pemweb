version: '3'
services:
  php_enep:
    build: ./php
    image: php:latest
    container_name: php_enep
    hostname: "php"
    volumes:
      - ./src-template:/var/www/html
      - ./php/www.conf:/usr/local/etc/php-fpm.d/www.conf
    working_dir: /var/www/html
    depends_on: 
      - mysql_enep

  mysql_enep:
    image: mariadb:10.2
    container_name: mysql_enep
    restart: unless-stopped
    tty: true
    ports:
      - "33386:3306"
    volumes:
      - ./database/data:/var/lib/mysql
      - ./conf.d:/etc/mysql/conf.d:ro
    environment:
      MYSQL_USER: Djambredd
      MYSQL_PASSWORD: 123
      MYSQL_ROOT_PASSWORD: 123
      SERVICE_TAGS: dev
      SERVICE_NAME: mysql  
    read_only: false

  nginx_enep:
    build: ./nginx
    image: nginx:latest
    container_name: nginx_enep
    hostname: "nginx"
    ports:
      - "92:80"
    volumes:
      - ./src-template:/var/www/html
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - php_enep
